{
  "name": "blog-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:frontend": "pnpm --filter frontend start",
    "start:backend": "pnpm --filter backend start:dev",
    "start:prod:backend": "pnpm --filter backend start:prod",
    "build:frontend": "pnpm --filter frontend build:prod"
  }
}